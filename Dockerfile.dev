# Base image
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source (in dev, we mount it instead via volume)
COPY . .

# Expose the Vite dev port
EXPOSE 5173

# Start dev server
CMD ["npm", "run", "dev"]
